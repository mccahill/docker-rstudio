before_script:
  - hostname
  - whoami
  - sudo docker --version

stages:
  - build
  - deploy_staging
  - deploy

build:
  tags: 
    - rstudio
    - labs-az-01
  stage: build
  script: 
    - echo "Building the app in staging"
    - pwd; ls -la
    - ./build-test


build_datafest:
  tags: 
    - rstudio
    - labs-az-10
  stage: build
  script: 
    - echo "Building the app in datafest staging"
    - pwd; ls -la
    - ./build-test
  only:
  - datafest_staging


deploy_datafest_staging_10:
  tags: 
    - rstudio
    - labs-az-10
  stage: deploy_staging
  script:
    - echo "Building the app in datafest staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to datafest staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: datafest_staging
  only:
  - datafest_staging


deploy_datafest_staging_11:
  tags: 
    - rstudio
    - labs-az-11
  stage: deploy_staging
  script:
    - echo "Building the app in datafest staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to datafest staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: datafest_staging
  only:
  - datafest_staging


deploy_datafest_staging_12:
  tags: 
    - rstudio
    - labs-az-12
  stage: deploy_staging
  script:
    - echo "Building the app in datafest staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to datafest staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: datafest_staging
  only:
  - datafest_staging


deploy_datafest_staging_13:
  tags: 
    - rstudio
    - labs-az-13
  stage: deploy_staging
  script:
    - echo "Building the app in datafest staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to datafest staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: datafest_staging
  only:
  - datafest_staging


deploy_datafest_staging_14:
  tags: 
    - rstudio
    - labs-az-14
  stage: deploy_staging
  script:
    - echo "Building the app in datafest staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to datafest staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: datafest_staging
  only:
  - datafest_staging


deploy_datafest_production_10:
  tags: 
    - rstudio
    - labs-az-10
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: datafest_production
  when: manual
  only:
  - datafest

deploy_datafest_production_11:
  tags: 
    - rstudio
    - labs-az-11
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: datafest_production
  when: manual
  only:
  - datafest

deploy_datafest_production_12:
  tags: 
    - rstudio
    - labs-az-12
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: datafest_production
  when: manual
  only:
  - datafest

deploy_datafest_production_13:
  tags: 
    - rstudio
    - labs-az-13
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: datafest_production
  when: manual
  only:
  - datafest


deploy_datafest_production_14:
  tags: 
    - rstudio
    - labs-az-14
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: datafest_production
  when: manual
  only:
  - datafest


deploy_staging_02:
  tags: 
    - rstudio
    - labs-az-02
  stage: deploy
  script:
    - echo "Building the app in staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: staging
  only:
  - staging


deploy_staging_03:
  tags: 
    - rstudio
    - labs-az-03
  stage: deploy
  script:
    - echo "Building the app in staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: staging
  only:
  - staging


deploy_staging_04:
  tags: 
    - rstudio
    - labs-az-04
  stage: deploy
  script:
    - echo "Building the app in staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: staging
  only:
  - staging


deploy_staging_08:
  tags: 
    - rstudio
    - labs-az-08
  stage: deploy
  script:
    - echo "Building the app in staging"
    - pwd; ls -la
    - ./build-test
    - echo "Deploy to staging server"
    - pwd; ls -la
    - ./run-test-instance
  environment:
    name: staging
  only:
  - staging


deploy_production_01:
  tags: 
    - rstudio
    - labs-az-01
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: production
  when: manual
  only:
  - master

deploy_production_02:
  tags: 
    - rstudio
    - labs-az-02
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: production
  when: manual
  only:
  - master

deploy_production_03:
  tags: 
    - rstudio
    - labs-az-03
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: production
  when: manual
  only:
  - master

deploy_production_04:
  tags: 
    - rstudio
    - labs-az-04
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: production
  when: manual
  only:
  - master

deploy_production_08:
  tags: 
    - rstudio
    - labs-az-08
  stage: deploy
  script:
    - echo "Building the production app"
    - pwd; ls -la
    - ./build-production
    - echo "Deploy to production server"
    - cd /srv/persistent-data/docker-scripts; ./run-everything
  environment:
    name: production
  when: manual
  only:
  - master
